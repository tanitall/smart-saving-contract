apply plugin: 'java'
apply plugin: 'idea'

sourceCompatibility = 1.7
version = '1.0'

repositories {
    mavenCentral()
    flatDir {
        dirs 'lib'
    }
}

dependencies {
    compile name: 'neo-devpack-java-2.3.0'
    testCompile group: 'junit', name: 'junit', version: '4.+'
}
